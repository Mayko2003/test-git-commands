Repo for practice git commands

# TAGS

git tag -> show all tags

git tag tag_name -> create a lightweight tag

git tag -a tag_name -m "description" -> create an annotated tag

git show tag_name -> show info about tag

git tag (-a) tag_name HASH_COMMIT -> create a tag for a specific commit